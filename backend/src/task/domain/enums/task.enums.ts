export enum TaskPriority {
    HIGH = 'high',
    MEDIUM = 'medium',
    LOW = 'low',
  }
  
  export enum TaskStatus {
    PENDING = 'pending',
    ACTIVE = 'active',
    COMPLETED = 'completed',
  }
  